apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: model-canary

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: model-canary
  app.kubernetes.io/part-of: model-canary
